name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1
    - name: Restore dependencies
      working-directory: ./dotNetRogueLootAPI
      run: ls 
    - name: Build
      run: dotnet build
    - name: Test
      run: dotnet test --no-build --verbosity normal
